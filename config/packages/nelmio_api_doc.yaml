nelmio_api_doc:
    documentation:
        info:
            title: Service with modules and DDD - demo app
            description: Service with modules and DDD - basic concepts demonstrator.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/customer
            - ^/order
            - ^/printer
